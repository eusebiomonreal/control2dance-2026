---
// Esta página se renderiza dinámicamente para poder hacer redirect
export const prerender = false;

// Redirigir a la página del carrito (checkout optimizado)
return Astro.redirect('/carrito');
---
